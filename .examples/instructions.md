# Sample Instructions

Add a new feature: a hello world endpoint.

1. Create a route `/hello` that returns `{"message": "Hello, World"}`
2. Add unit tests for the endpoint
3. Ensure all existing tests pass
